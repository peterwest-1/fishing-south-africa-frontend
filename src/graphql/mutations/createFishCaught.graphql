mutation CreateFishCaught($input: FishCaughtInput!) {
  createFishCaught(input: $input) {
    fishCaught {
      id
      userId
      species
      weight
      length
      location
      notes
    }
    errors {
      field
      message
    }
  }
}
